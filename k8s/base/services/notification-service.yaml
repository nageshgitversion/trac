# ═══════════════════════════════════════════════════════════
# INVESTRAC — notification-service
# Port: 8088 | Replicas: 2
# ═══════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: investrac
  labels:
    app: notification-service
    version: "1.0"
    app.kubernetes.io/part-of: investrac
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0       # Zero-downtime deployment
  template:
    metadata:
      labels:
        app: notification-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8088"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: notification-service
          image: investrac/notification-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8088
              name: http
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 400m
              memory: 768Mi
          env:
            - name: SERVER_PORT
              value: "8088"
            - name: DB_HOST
              value: "mysql-notification"
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: investrac-config
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: investrac-secrets
                  key: db-password
            - name: EUREKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: investrac-config
                  key: EUREKA_HOST
            - name: EUREKA_USER
              valueFrom:
                configMapKeyRef:
                  name: investrac-config
                  key: EUREKA_USER
            - name: EUREKA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: investrac-secrets
                  key: eureka-password
            - name: KAFKA_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: investrac-config
                  key: KAFKA_SERVERS
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: investrac-config
                  key: REDIS_HOST
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: investrac-secrets
                  key: redis-password
            - name: ZIPKIN_HOST
              valueFrom:
                configMapKeyRef:
                  name: investrac-config
                  key: ZIPKIN_HOST
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: investrac-secrets
                  key: jwt-public-key

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8088
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8088
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]  # Allow in-flight requests to complete
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: investrac
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 8088
      targetPort: 8088
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service-hpa
  namespace: investrac
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
